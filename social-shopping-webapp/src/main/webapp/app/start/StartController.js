define(['petStartModule'], function (module) {
    'use strict';

    module.controller('StartController', [
        '$scope',
        '$location',
        '$filter',
        '$modal',
        function ($scope, $location, $filter, $modal) {}
    ]);
});
